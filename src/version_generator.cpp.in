#include "cglass/version_generator.hpp"
/* This file should be configured by CMake to generate a cpp file that gets
   compiled to make the cglass_version variable (the most recent git commit
   sha1) available to any file that includes the above header */
#define GIT_SHA1 "@GIT_SHA1@"
const char cglass_version[] = GIT_SHA1;
